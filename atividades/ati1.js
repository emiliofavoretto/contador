const multiplos = (n) => n * 2;

const saudar = (nome) => ("ola " + nome + "!")

const somar = (a,b) => a + b;

const EhPar = (n) => n % 2 === 0;

const converter = (celsius) => celsius * 9/5 + 32;




/*function multiplos(n) {
    return n * 2;
}

console.log(n)*/